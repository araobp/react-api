/**
 * Box__c table synchronized with Box object on Salesforce Cloud
 */
export type Box__c = {
    id__c: number;
    move__c: boolean;
}

export type Box_Stats = {
    id__c: number;
    count: number
}